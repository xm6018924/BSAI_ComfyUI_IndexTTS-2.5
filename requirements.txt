# BSAI_ComfyUI_IndexTTS-2.5 dependencies
# Install these in your ComfyUI Python environment

# Core TTS engine (NOT on PyPI - installed from GitHub source)
# indextts is installed via: pip install --no-deps --ignore-requires-python --no-build-isolation git+https://github.com/index-tts/index-tts.git
# See install_bsai_indextts.bat for automated installation

# Audio processing (already included in ComfyUI environment)
torchaudio>=2.0.0
numpy>=1.24.0

# Config
omegaconf>=2.3.0

# Model download (at least one should be available)
modelscope>=1.18.0
huggingface_hub[cli,hf_xet]>=0.25.0

# Optional: DeepSpeed for faster inference (Linux recommended)
# deepspeed>=0.14.0

# Optional: Flash attention for GPT2 acceleration
# flash-attn>=2.5.0
